import random

computer_choice = random.choice(['h', 't'])

user_choice = input("Enter your choice (h or t): ")

if user_choice == computer_choice:
    print("You win")
else:
    print("Bad luck")

print(f"The computer selected {computer_choice}")
